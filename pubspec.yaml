name: spring_view
description: A Flutter package that provides an animated button with a delightful bounce effect. Easily integrate it into your app for a playful touch.
version: 0.0.3
homepage: https://github.com/khuship745/spring_view.git

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
